/// <reference types="react" />
import { Step } from "code-surfer-types";
declare type CodeSurferProps = {
    steps: Step[];
    progress: number;
    tokens: string[][];
    types: string[][];
    maxLineCount: number;
    showNumbers?: boolean;
};
export declare function CodeSurfer({ progress, steps, tokens, types, maxLineCount, showNumbers }: CodeSurferProps): JSX.Element;
export {};
