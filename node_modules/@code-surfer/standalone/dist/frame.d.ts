/// <reference types="react" />
import { Step, Dimensions } from "code-surfer-types";
declare type ContainerProps = {
    progress: number;
    dimensions?: Dimensions;
    steps: Step[];
    tokens: string[][];
    types: string[][];
    maxLineCount: number;
    showNumbers: boolean;
};
declare function CodeSurferContainer({ progress, dimensions, steps, tokens, types, maxLineCount, showNumbers }: ContainerProps): JSX.Element;
export default CodeSurferContainer;
